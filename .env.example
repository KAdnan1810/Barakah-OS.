# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# AI Assistant (server-side only — never exposed to the client in production;
# route AI calls through a Supabase Edge Function, not directly from the browser)
# ANTHROPIC_API_KEY=

# App
VITE_APP_NAME="Barakah OS"
VITE_APP_ENV=development
